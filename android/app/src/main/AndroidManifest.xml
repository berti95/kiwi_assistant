<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      setAlarmClock está exento de SCHEDULE_EXACT_ALARM en la
      mayoría de versiones, pero algunos OEM de Android 14+ lo
      exigen igualmente. SCHEDULE_EXACT_ALARM se concede por
      defecto en instalación; USE_EXACT_ALARM es la variante "no
      revocable" para apps con uso legítimo de alarma. Declaramos
      ambas para cubrir cualquier configuración.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES"
        tools:ignore="ProtectedPermissions" />

    <!--
      Android 11+ requiere declarar visibilidad de paquetes terceros
      que vayamos a lanzar con PackageManager.getLaunchIntentForPackage.
      Sin esto, la llamada devuelve null aunque la app esté instalada.
      Añadir aquí cada paquete que orquestamos vía device_command.
    -->
    <queries>
        <package android:name="com.spotify.music" />
        <package android:name="com.google.android.youtube" />
    </queries>

    <application
        android:name=".KiwiApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kiwi">

        <receiver
            android:name=".updater.InstallResultReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kiwi.assistant.UPDATE_INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".alarm.AlarmRingReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.kiwi.assistant.ALARM_RING" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Kiwi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
